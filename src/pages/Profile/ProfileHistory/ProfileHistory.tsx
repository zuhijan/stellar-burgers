import React, { FC } from "react";

interface IProfileHistory {}

const ProfileHistory: FC<IProfileHistory> = () => {
  return <div style={{ width: "480px", height: "344px" }}>ProfileHistory</div>;
};
export default ProfileHistory;
