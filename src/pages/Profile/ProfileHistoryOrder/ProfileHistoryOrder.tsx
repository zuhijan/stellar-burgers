import React, { FC } from "react";

interface IProfileHistoryOrder {}

const ProfileHistoryOrder: FC<IProfileHistoryOrder> = (props) => {
  return <div>ProfileHistoryOrder</div>;
};
export default ProfileHistoryOrder;
